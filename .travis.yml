language: cpp 
compiler: gcc
sudo: true
  
addons: 
  apt: 
    packages: 
      - libboost-all-dev

before_script: 
  - mkdir build 
  - cd build 
  - cmake ..

script: 
  - export CC=gcc-7
  - make && make cife_main_tests
